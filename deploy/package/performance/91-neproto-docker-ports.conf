# Docker host networking shares the host network namespace. Keep the NP/2 and
# Caddy containers non-root and capability-free while allowing their dedicated
# processes to bind the standards-compliant UDP/TCP ports 80 and 443.
net.ipv4.ip_unprivileged_port_start=0
