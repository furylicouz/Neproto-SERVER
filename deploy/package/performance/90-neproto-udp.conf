# QUIC and WebRTC request multi-megabyte UDP socket buffers. Keep the global
# defaults unchanged, but allow those transports to reserve what they need.
net.core.rmem_max=7500000
net.core.wmem_max=7500000
